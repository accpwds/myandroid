<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <!-- https://developer.android.google.cn/reference/android/Manifest.permission.html  完整权限表 -->

    <!-- 访问系统网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 监听开机广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!-- 访问手机账户 -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- 读取联系人 -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <!-- 读取应用列表 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.android.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.android.FirstActivity" />
        <activity android:name="com.example.android.MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.VIEW" /> -->
            <!-- <category android:name="android.intent.category.DEFAULT" /> -->
            <!-- <data android:scheme="http" /> &lt;!&ndash; 弹出选择浏览方式的形式打开 &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name="com.example.android.FruitActivity"
            android:label="@string/linear" />
        <activity
            android:name="com.example.android.FruitActivity2"
            android:label="@string/linear" />
        <activity
            android:name="com.example.android.FruitActivity3"
            android:label="@string/staggeredgrid" />
        <activity
            android:name="com.example.android.MsgActivtiy"
            android:label="聊天对话框" />
        <activity
            android:name="com.example.android.BroadCastActivity"
            android:label="广播" />
        <activity android:name="com.example.android.LocalBroadcastReceiver" />
        <activity android:name="com.example.android.SQLiteDatabaseActivity"></activity>
        <activity android:name=".LitePalActivity"></activity>

        <!--  -->
        <receiver
            android:name="com.example.android.BootCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.example.android.MyBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.test1.MY_BROADCAST" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.example.android.AnotherBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="com.example.test1.MY_BROADCAST" />
            </intent-filter>
        </receiver>

        <activity android:name="com.example.android.SaveFileActivity" />
        <activity android:name="com.example.android.SharedPreferencesActivity"></activity>
    </application>

</manifest>